# Resiliency

目标：说明依赖失败、重复请求、并发冲突和发布异常时系统如何恢复。

至少判断：外部调用超时、错误传播、降级、重试范围/次数/退避、幂等、去重、部分成功、事务与补偿、并发控制、数据/API/消息兼容、发布顺序、回滚、日志、指标、告警和责任人。

禁止没有超时的外部调用和无限重试。数据结构、消息格式或公共 API 变化必须写兼容窗口。回滚不安全时要明确标记，并提供替代恢复办法。

纯文档或没有运行时依赖的离线工具可以写 N/A，但理由必须明确说明不存在相应故障面。部署后的新问题进入下一 Loop，不新增 Operations 阶段。
