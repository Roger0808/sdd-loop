# Architecture Baseline 写作口径

这是长期架构的内容清单，不是要提前创建的空文档。只有在 Architecture 阶段完成真实勘察后才落盘。

- 系统定位、边界和对外责任。
- 用 Mermaid 或 ASCII 表示的系统/模块关系图。
- 核心模块、所有者、依赖方向与禁止依赖。
- 入站/出站接口、外部系统和关键数据流。
- 数据所有权、存储、一致性、迁移与兼容边界。
- 运行时、部署拓扑、配置与安全边界。
- 关键验收和可观测入口。
- 每条事实的证据来源；无法验证的内容标为待勘察，不写成既成事实。

路径选择：已有 `docs/architecture/` 结构优先，不改名；无旧结构时，单系统用 `docs/architecture/overview.md`，分流用 `docs/architecture/<stream>.md`。只在存在真实跨系统关系时再建总体 `overview.md`。
