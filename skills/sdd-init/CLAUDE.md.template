# CLAUDE.md

本仓库按 **SDD Loop** 推进。规则只有一份，在 [AGENTS.md](AGENTS.md) ——**动手前先读它**，本文件不重复维护任何条款。

重复一份就会漂移，漂移之后没人知道哪份算数。所以这里只做三件事：指路、给入口、给命令。

## 入口

| 要什么 | 去哪 |
|---|---|
| 门禁规则、事实优先级、文档规则 | [AGENTS.md](AGENTS.md) |
| 当前 Loop 状态（唯一状态源） | `docs/loops/status.md` |
| 当前 Loop 的阶段文档 | `docs/loops/loop-<N>/` |
| 已关闭 Loop（只用于追溯，不是规范入口） | `docs/archive/` |

## 每轮开局

```bash
sdd-loop check
```

把状态文件的「声明」和文件里的「事实」摆在一起比。退出码：`0` 干净 / `1` 声明与事实不符 / `2` 判据读不出来（此时不给任何结论）。

它只读，不改任何文件。报出矛盾时按 AGENTS.md 的规则办：**停止相关工作并请求用户确认**，不要自己猜一个状态接着做。

## 写条款之前

```bash
sdd-loop guide --type <文档>.<条款>
```

给出该类条款该写哪几项、本仓库现有编号族（新增条款沿用同族前缀，不另起体系）和一条参考写法。不带 `--type` 列出全部类型。
